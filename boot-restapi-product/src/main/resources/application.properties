# MySql credential 
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/mydb?allowPublicKeyRetrieval=true&useSSL=false&erverTimezone=GMT
spring.datasource.username = root
spring.datasource.password = Mysql@810

# Server configuration
server.servlet.context-path=/product-rest-api

# rest api configuration
productrestapi.services.url=http://localhost:8080/product-rest-api/products/

## Hibernate Properties
#database query print
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect


